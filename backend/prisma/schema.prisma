// ScholarClaw Prisma Schema
// Database: PostgreSQL

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider             = "prisma-client-py"
  interface            = "asyncio"
  recursive_type_depth = 5
}

// ============================================
// User Model - Core authentication entity
// ============================================
model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  fullName  String   @map("full_name")
  role      String   @default("user")
  isActive  Boolean  @default(true) @map("is_active")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // Relations (to be added)
  // profile      Profile?
  // applications Application[]
  // auditLogs    AuditLog[]

  @@map("users")
}

// ============================================
// Additional models to be added:
// - Profile (user profile with eligibility data)
// - Scheme (scholarship schemes)
// - Application (user applications to schemes)
// - AuditLog (hash chain audit entries)
// - AttackLog (security incident logs)
// - RefreshToken (JWT refresh tokens)
// ============================================
